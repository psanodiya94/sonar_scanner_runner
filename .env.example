# Sonar Scanner Runner - Environment Variables
# Copy this file to .env and update with your settings

# SonarQube Configuration
SONAR_HOST_URL=http://localhost:9000
SONAR_TOKEN=your_sonarqube_token_here

# Server Configuration
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# Workspace Directory
WORKSPACE_DIR=./temp

# Build Tools
BUILD_WRAPPER_CMD=build-wrapper-linux-x86-64
SONAR_SCANNER_CMD=sonar-scanner

# Logging
LOG_LEVEL=INFO

# Build Prerequisites
# Note: For complex prerequisite configurations, use config.json instead
# The config.json file supports per-build-system prerequisites (maven, gradle, npm, etc.)
# that will be executed in the same terminal session before the build command
